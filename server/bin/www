#!/usr/bin/env node

/**
 * Module dependencies.
 */
var reapp = require('express')();
var app = require('../app');
var debug = require('debug')('server:server');
var http = require('http');
var https = require('https');
var certOpts = require('../conf').certOpts;



/**
 * Get port from environment and store in Express.
 */


/**
 * Create HTTP server.
 */
//reapp.all("*",(req,res,next)=>{
//   res.redirect("https://"+req.headers.host+"/"+req.path);
//})
//var httpServer = http.createServer(reapp);
//httpServer.listen('8080','0.0.0.0');

/**
 * Listen on provided port, on all interfaces.
 */
var httpsServer = https.createServer(certOpts, app);
httpsServer.listen('8443','0.0.0.0');
